import { LinearGradient } from 'expo-linear-gradient';
import styled from 'styled-components';

/* export default styled(LinearGradient).attrs({
  colors: ['#00E6FF', '#17171f'],
})`
  flex: 1;
`; */

export default styled.View`
  background-color: #17171f;
  flex:1;
`;
